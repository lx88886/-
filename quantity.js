let value='';const output=document.querySelector('#count'),confirm=document.querySelector('#confirm');document.querySelector('.pad').addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;if(b.dataset.delete)value=value.slice(0,-1);else if(value.length<3)value=(value+b.textContent).replace(/^0+(?=\d)/,'');if(+value>200)value='200';output.textContent=value||'0';confirm.disabled=!value||+value<1;confirm.textContent=confirm.disabled?'最多 200 个':'确认发送'});confirm.addEventListener('click',()=>location.href='sign.html');
