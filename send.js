const track=document.querySelector('.track');let index=1,startX=0,dragging=false;function render(){track.style.transform=`translateX(${-index*750}px)`}function start(e){dragging=true;startX=(e.touches?e.touches[0]:e).clientX;track.style.transition='none'}function end(e){if(!dragging)return;const x=(e.changedTouches?e.changedTouches[0]:e).clientX;if(x-startX>45)index=Math.max(0,index-1);if(x-startX<-45)index=Math.min(2,index+1);dragging=false;track.style.transition='transform .28s ease';render()}track.addEventListener('touchstart',start,{passive:true});track.addEventListener('touchend',end);track.addEventListener('pointerdown',start);track.addEventListener('pointerup',end);document.querySelector('#next').addEventListener('click',()=>location.href='quantity.html');
